# 项目需求说明书：MediaGrabber（网页与屏幕媒体抓取工具）

> 这是一份完整的项目规格。请你（Claude Code）通读全文后按「开发流程要求」一节执行。
>
> **当前进度：阶段 0、1、2 已完成并通过验收。详见文末第 13 节。**

---

## 0. 给你的工作方式要求（最重要，请先看这节）

1. **我是编程新手。** 全程用中文回答。凡是需要我手动操作的环节（装环境、改系统设置、点哪个按钮、复制什么路径），都要写成一步一步的清单，不要假设我知道。
2. **严格按阶段推进，不要跳步。** 见「第 8 节 分阶段开发计划」。每完成一个阶段，停下来给我一份**验收清单**（我该运行什么命令、该看到什么现象、看到什么就说明这一阶段成了），等我确认通过再进入下一阶段。
3. **第一步不要写代码。** 先输出第 8 节里「阶段 0」要求的东西。
4. 每个关键函数写中文注释，说明它做什么、参数是什么、什么情况会失败。
5. 不要静默失败。任何错误都要往界面上报或写进日志，不许 `except: pass`。
6. 遇到本文档没写清楚的地方，先问我，不要自己拍板。

---

## 1. 项目是什么

一个常驻 Windows 系统托盘的**桌面应用**（不是浏览器扩展），用来把网页里和屏幕上的图片、视频保存到 D 盘。

有两种工作模式：

| | 模式一 · 浏览器模式 | 模式二 · 通用模式 |
|---|---|---|
| 抓什么 | 网页 DOM 里的真实图片/视频文件 | 屏幕上显示的任意画面区域 |
| 拿到的是 | 原始文件，原分辨率 | 屏幕像素截图，屏幕分辨率 |
| 适用 | 图库、商品页、社交媒体 | PDF 阅读器、本地软件、任何截不到源文件的场景 |

**一个必须先想明白的前提**：模式二拿到的只能是屏幕像素，不存在"原图"这个概念。1080p 屏幕上显示的 4K 图，存下来就是 1080p。这是操作系统层面的限制，不要试图绕过，也不要在界面上暗示能拿到原图。

---

## 2. 技术栈（硬约束，不要替换）

- **Python 3.11+**
- GUI：**PySide6**
- 截图：**mss**
- 图像处理：**opencv-python** + **Pillow**
- 浏览器控制：**playwright** 的 `connect_over_cdp`（不要用 selenium，不要用 playwright 自己 launch 浏览器）
- HTTP 兜底下载：**httpx**
- 全局热键：**pynput**
- 打包：**PyInstaller**（最后阶段才做）

不要引入 Electron、不要用任何 Web 前端框架、不要引入打包构建工具。

---

## 3. 目录结构

```
media-grabber/
├── main.py                  # 入口：托盘图标、模式调度、全局异常捕获
├── config.py                # 配置读写封装
├── config.json              # 用户配置（首次运行自动生成）
├── requirements.txt
├── core/
│   ├── downloader.py        # 统一的下载与落盘（两模式共用）
│   ├── naming.py            # 文件名清洗、去重、路径拼接
│   └── history.py           # 保存记录
├── browser/
│   ├── controller.py        # 受控 Chrome 的启动与 CDP 连接管理
│   └── scanner.py           # 页面媒体提取
├── screen/
│   ├── overlay.py           # 全屏框选遮罩窗口
│   └── detector.py          # OpenCV 图片区域识别
├── ui/
│   ├── main_window.py       # 主窗口 + 缩略图网格
│   ├── settings.py          # 设置面板
│   └── toast.py             # 右下角浮动提示
├── chrome-profile/          # 受控 Chrome 的配置目录（自动生成，不要提交到 git）
├── logs/                    # 日志（自动生成）
└── README.md
```

---

## 4. 功能规格

### 4.1 模式一 · 浏览器模式

**启动浏览器**
- 检测 `127.0.0.1:9222` 是否可连
- 不通则自动定位本机 Chrome 安装路径（查注册表 + 常见路径兜底），用 `subprocess` 启动，参数：
  ```
  --remote-debugging-port=9222
  --user-data-dir=<程序目录>\chrome-profile
  ```
- 用 `playwright.chromium.connect_over_cdp("http://127.0.0.1:9222")` 连接
- 主窗口顶部有一个连接状态指示灯：灰（未启动）/ 黄（连接中）/ 绿（已连接）
- 我手动关掉浏览器时要能感知，指示灯变灰并提示，**程序不许崩溃**

**扫描页面**
- 点「扫描当前页面」，对当前活动标签页 `evaluate` 一段 JS，返回页面内所有媒体：
  - 最终 URL、`naturalWidth`/`naturalHeight`（视频用 `videoWidth`/`videoHeight`）、标签名、`alt`/`title`
- 地址提取要健壮：
  - `srcset` 存在时，解析并取**最高分辨率**那一项
  - `src` 是占位图（尺寸极小、base64、含 `placeholder`/`loading` 字样）时，回退到 `data-src` / `data-original` / `data-lazy-src` / `data-actualsrc`
  - 提取元素的 CSS `background-image`
  - `<picture>` 里的 `<source>` 也要处理
- 过滤：小于设定尺寸的图（默认 200×200）、base64 小图标，不进列表

**选择与下载**
- 缩略图网格展示，每项显示尺寸、格式、文件大小（能拿到就显示）
- 每项带复选框；底部有全选 / 反选 / 按尺寸滑块筛选
- 「下载选中」→ 进度条 → 完成后汇总「成功 N 张，失败 M 张」
- 失败项在网格里标红边框，鼠标悬停显示失败原因

### 4.2 模式二 · 通用模式

**唤起**
- 全局热键 `Ctrl+Alt+S`（可在设置里改）
- 先用 mss 截取当前屏幕，把这张截图铺满全屏做成无边框置顶窗口——**必须先冻结画面**，否则框选时底下内容会动

**手动子模式（默认）**
- 拖拽画半透明蓝色矩形，实时在鼠标旁显示选区像素尺寸
- 松开鼠标即保存为 PNG
- 右键取消当前框，`Esc` 退出遮罩

**自动子模式（`Tab` 切换）**
- 对冻结画面做：灰度 → 高斯模糊 → Canny 边缘检测 → `findContours` → 筛选矩形
- 筛选条件全部写进 config，可调：最小面积、宽高比范围上下限、矩形度阈值
- 候选区域画细黄框；鼠标悬停变粗；点击选中变绿色实线；再点取消
- 支持多选，`Ctrl+A` 全选，`Enter` 批量保存
- 自动模式下**也允许手动补画矩形**（识别一定会有漏检，必须留人工兜底）

**保存反馈**
- 屏幕右下角弹出提示条，显示文件名和保存路径，2 秒后淡出

### 4.3 主窗口与托盘

- 托盘右键菜单：显示主窗口 / 设置 / 打开保存目录 / 退出
- 主窗口：模式切换标签页、设置入口、历史记录面板
- 历史面板：时间、来源、路径、缩略图；点击可在资源管理器中定位到该文件

---

## 5. 保存规则（两模式共用一套逻辑，放在 `core/naming.py`）

- 根目录默认 `D:\MediaGrabber`，设置里可改
- 浏览器模式：`<根目录>\<域名>\<YYYY-MM-DD>\`
- 通用模式：`<根目录>\Screen\<YYYY-MM-DD>\`
- 是否按域名分、是否按日期分，都做成配置开关
- 文件名清洗：非法字符 `\ / : * ? " < > |` 替换为下划线；超长截断到 100 字符（保留扩展名）
- 通用模式文件名：`screen_20260830_143052.png`
- 重名自动加 `_1` `_2` 后缀，**绝不覆盖已有文件**
- 按文件内容 MD5 去重，重复的跳过并在界面提示「已存在，已跳过」
- 目标目录不存在时自动创建；创建失败（盘符不存在、无写权限）要明确报错，不要闷掉

---

## 6. 登录站点的处理（浏览器模式的关键部分）

**背景**：从 Chrome 136 起，`--remote-debugging-port` 在针对默认配置目录时会被直接忽略，必须搭配指向非标准目录的 `--user-data-dir`。所以**无法复用我平时那个 Chrome 的登录状态**，只能在受控浏览器里单独登录一次。这不是 bug，是 Chrome 的安全策略，不要试图绕过。

要求：

1. `chrome-profile` 文件夹**持久保留**，绝不每次启动清空。登录一次，之后一直有效。
2. 首次启动（检测到该文件夹不存在）时，主窗口顶部显示黄色提示条：
   > 首次使用：请在弹出的浏览器中登录你需要抓取的网站。登录状态会自动保存，以后不用重复登录。

   点「知道了」后消失，之后不再出现。
3. **下载已登录站点的资源时，不要用独立 httpx 客户端手动拼 cookie。**
   统一走 `page.request.get(url)`——它共享 browser context 的完整会话状态，cookie、Referer、UA 全部自动正确，这是登录站点能下载成功的核心。
   - 小文件直接取 body 写盘
   - 大于 20MB 走流式写入，不许整个读进内存
   - `page.request` 失败时，降级用 httpx 手动带 Referer + UA 重试一次
4. 设置面板里加「登录状态」区域：通过 `context.cookies()` 聚合出当前有 cookie 的域名列表，让我一眼看出哪些站点已登录。旁边一个「清除登录数据」按钮，删除 `chrome-profile`，需二次确认。
5. 下载返回 401/403 时，错误提示写明「可能是登录已过期，请在受控浏览器中重新登录该网站」，不要只显示状态码。

---

## 7. 必须处理的技术难点

方案阶段请逐条说明你的解法，写清楚用哪个库的哪个 API。

| # | 难点 | 说明 |
|---|---|---|
| 1 | Chrome 136+ 调试端口限制 | 见第 6 节。`--user-data-dir` 是强制参数 |
| 2 | 防盗链 | 大量站点校验 Referer。首选走 `page.request`；httpx 兜底时必须手动带 Referer + 浏览器 UA |
| 3 | 懒加载图片 | 见 4.1 的地址提取规则 |
| 4 | `blob:` / `data:` 地址 | 外部取不到，必须在页面内 `fetch` 成 ArrayBuffer → base64 → 传回 Python 端解码写盘 |
| 5 | **多显示器 + DPI 缩放** | Windows 高危区。截图物理像素坐标和 Qt 逻辑坐标经常对不上。遮罩窗口必须能正确覆盖任意一块屏幕，框选坐标要能准确换算回物理像素。这一块请单独说明你的处理方案，并给我验证方法 |
| 6 | 画面冻结 | 见 4.2 |
| 7 | 自动识别的误检漏检 | 必然存在。阈值可调 + 允许手动补框，不要指望调参调到完美 |
| 8 | 全局热键与 Qt 事件循环冲突 | pynput 监听在独立线程，唤起 UI 必须通过 Qt 信号槽切回主线程，不许跨线程直接操作控件 |
| 9 | 签名过期链接 | 带 token 的直链（如 S3 预签名）有时效，扫描完要尽快下载。失败提示里要提到这个可能性 |
| 10 | DRM 加密内容 | 无法抓取。识别到明显是 DRM 流时直接提示不支持，不要反复重试 |

---

## 8. 分阶段开发计划

### 阶段 0 · 方案设计（先做这个，不要写代码）

输出：
1. 整体架构说明：两种模式下，数据各自怎么从源头流到磁盘
2. 第 7 节 10 个难点各自的解决方案
3. 你对本计划的调整建议（如果你觉得某个阶段顺序不合理）
4. 环境准备清单：Python 版本、pip 包及版本、Chrome 版本要求、我需要提前建的文件夹
5. **你认为本项目最容易翻车的地方是哪个，为什么**

我确认后再开始。

### 阶段 1 · 通用模式最小可用版

范围：只做手动框选，打通「热键 → 冻结画面 → 拖框 → PNG 落到 D 盘」。

交付：`config.py` / `config.json` / `core/naming.py` / `core/downloader.py`（只需 `save_bytes`）/ `screen/overlay.py` / `ui/toast.py` / `main.py`（托盘 + 热键）/ `README.md`

**重点**：多显示器和 DPI 缩放必须在这一阶段就处理对，写完明确告诉我怎么验证坐标准不准。

验收：在主屏和副屏各框选一次，文件正确出现在 `D:\MediaGrabber\Screen\<今天>\`，且截出来的内容和我框的范围完全一致。

### 阶段 2 · 浏览器模式

范围：受控 Chrome 启动 + CDP 连接 + 页面扫描 + 缩略图网格 + 批量下载 + 第 6 节的登录支持。

交付：`browser/controller.py` / `browser/scanner.py` / `ui/main_window.py` / `core/downloader.py` 扩展

并发上限 3，每请求间隔 200ms，失败重试 2 次。

验收：分别在一个免登录图站和一个需要登录的站点各测一次，都能正确批量下载。

### 阶段 3 · 自动识别 + 打磨

范围：`screen/detector.py` 自动框图 + `core/history.py` 历史记录 + 设置面板 + 全局异常捕获与日志。

日志写到 `logs/`，按天分文件，出问题时我可以直接发给你看。

### 阶段 4 · 打包

PyInstaller 打成单 exe，处理好 PySide6 和 opencv 的依赖收集问题。这一阶段等我单独说了再做。

---

## 9. 工程要求

- 所有路径操作用 `pathlib`，不要手拼字符串
- 配置有默认值，`config.json` 损坏或字段缺失时能优雅降级而不是崩溃
- 网络请求、文件写入、CDP 调用全部要有超时和异常处理
- 主线程绝不阻塞：下载、扫描、识别都放 `QThread` 或线程池，通过信号更新 UI
- 不要写单元测试框架，但每阶段给我一份手动验收清单

---

## 10. 环境准备（请在阶段 0 补全并核对）

1. Python 3.11+（`python --version` 检查）
2. Chrome 浏览器（版本 136 以上，本项目的实现假定了新版行为）
3. 手动建好 `D:\MediaGrabber` 文件夹
4. `pip install -r requirements.txt`
5. `playwright install chromium`（如果你的方案里需要）

---

## 11. 我遇到问题时的沟通格式

我会这样发给你，你按这个套路回：

> 【现象】……
> 【我已经试过】……

你先列 2–3 种可能原因和各自的排查方法，**再**给修改方案。改动尽量小、尽量集中，不要一次重写一大片。

---

## 12. 使用边界

这个工具用来抓取我本人有权访问的内容。实现时请注意：

- 批量下载必须有并发上限和请求间隔，不要把目标站点打崩
- 不要实现任何绕过登录、绕过付费墙、破解 DRM 的功能，遇到这类情况直接提示"不支持"
- 界面上不要出现鼓励大规模爬取的措辞

---

## 13. 开发进度（每完成一个阶段更新这里）

| 阶段 | 状态 | 验收情况 |
|---|---|---|
| 阶段 0 · 方案设计 | ✅ 完成 | 2026-08-30 确认 |
| 阶段 1 · 通用模式（手动框选） | ✅ 完成 | 2026-08-30 验收通过，坐标标定三连测全过 |
| 阶段 2 · 浏览器模式 | ✅ 完成 | 2026-08-31 验收通过，pixabay（免登录）+ pixiv（登录）均能批量下载 |
| 阶段 3 · 自动识别 + 打磨 | ⬜ 未开始 | detector.py / 历史面板 / 设置面板 |
| 阶段 4 · PyInstaller 打包 | ⬜ 未开始 | 用户已明确表示最终形态是双击 exe |

### 与原规格的偏差（都已和用户确认）

1. **视频只做直链**。`<video src="xxx.mp4">` 和 `<source>` 指向的真实文件可以下载；
   HLS/DASH 分片流（`blob:` 地址）明确提示「不支持」，不重试。合并分片需要
   ffmpeg，不在既定技术栈内。

2. **`page.request` 无法流式下载**。规格第 6 节要求「统一走 `page.request`，
   大于 20MB 走流式写入」，但 playwright 的 `APIResponse` 只有一次性返回全部
   字节的 `body()`，没有流式接口，两者无法同时满足。实际做法：图片走
   `page.request`（会话保真度最高），视频走 httpx 流式（边下边写，不爆内存），
   两条路径共用 `core/downloader.py` 里的去重、重名避让、历史记录逻辑。

3. **日志 + 全局异常捕获从阶段 3 提前到阶段 1**。热键和 DPI 出问题时的症状是
   「没反应」「偏了一点」，没有日志无法远程定位。

4. **依赖用 `PySide6-Essentials` 而不是完整的 `PySide6`**。本项目只用
   QtCore/QtGui/QtWidgets，全在 Essentials 里；完整包会多拉约 130MB 的 Addons
   （QtWebEngine、Qt3D、QtCharts 等）一个都用不到。已验证 webp 解码正常。
   将来真需要 Addons，`pip install PySide6` 即可补上，不用改代码。

5. **新增了 4 个规格目录结构里没有的文件**，理由见 README 第八节：
   `screen/geometry.py`、`screen/capture.py`、`tools/env_check.py`、`tools/dpi_check.py`。

### 踩过的坑（改动相关代码前务必先看这里）

1. **进程 DPI 感知模式只能设一次，先到先得。**
   `mss` 初始化时会调 `SetProcessDPIAware()`（老的 system-aware 模式）。必须在
   `main.py` 最早期主动设成 Per-Monitor V2 把位置占掉，否则 Qt 的几何计算全盘
   错位——**不报错、不崩溃，只表现为框选位置偏一点**。

2. **不能信 Qt 报的 `devicePixelRatio`。**
   实测 2560×1600 @150%：Qt 报 DPR=1.5，真实系数是 `sx=1.499707 / sy=1.499531`。
   必须用「物理宽 ÷ 逻辑宽」现算。

3. **绘制物理像素图必须先把画笔切进设备像素坐标系**（`painter.scale(1/dpr, 1/dpr)`），
   否则 150% 这种非整数缩放下会被重采样，错开半个像素。

4. **`naturalWidth` 对 srcset 图片是密度换算过的值，不是文件真实尺寸。**
   同一张 1400×1000 的图：放在 1036px 宽的位置报 1036（缩小），放在 1707px 宽的
   位置报 1707（放大）。**两个方向都会偏**，必须用 `srcset` 的 `w` 描述符还原。

5. **占位图关键词匹配必须卡边界。** 裸的子串匹配会把 `lazy.png`、`blanket.jpg`
   这类正常图片误杀，而且极其隐蔽——图明明在页面上，扫描就是抓不到。

6. **`page.request` 不走 Chrome 的网络栈。** 它是 Playwright 的 Node 进程自己发的
   请求，不用 Chrome 的代理、自己做 DNS。DNS 被污染或需要代理时，页面上图片
   显示正常，它却 `connect ETIMEDOUT`。必须有 httpx 兜底通道。

7. **代理不能只看环境变量。** 代理软件常常只改 Windows 系统代理设置不设环境变量；
   就算设了，从资源管理器双击启动的进程也继承不到。必须像 Chrome 一样读注册表
   `Internet Settings`。

8. **httpx 访问 127.0.0.1 也会走代理。** 探测 Chrome 调试端口时必须
   `trust_env=False`，否则代理返回 502，表现为「Chrome 起来了却连不上」。
